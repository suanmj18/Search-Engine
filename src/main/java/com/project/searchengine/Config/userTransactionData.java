package com.project.searchengine.Config;

import com.project.searchengine.Transactions.TransactionManager;
import com.project.searchengine.Transactions.TransactionType;

public class userTransactionData {

    //place it where login of user happens
    //TransactionManager.insert(user,null, TransactionType.LOGIN);

    //place it where logout of user happens
    //TransactionManager.insert(user,null, TransactionType.LOGOUT);
}
